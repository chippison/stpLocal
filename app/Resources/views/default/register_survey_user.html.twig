 {% extends 'landing.html.twig' %}
 {% block main_body %}
    <div class="section" style="min-height: 75vh;"> 
            <div class="filter-black"></div>
                <div class="container">
                    <div class="row">
                        <div class="col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3 col-xs-10 col-xs-offset-1 ">
                            <div class="register-card">
                                <h3 class="title">{{ lbl }} Survey</h3>
                                {{ form_start(form) }}
                                    <div style="margin-top: 10px">
                                    {{ form_errors(form.t_email.first) }}
                                    {{ form_widget(form.t_email.first) }}
                                    </div>
                                    <div style="margin-top: 10px">
                                    {{ form_errors(form.t_email.first) }}
                                    {{ form_widget(form.t_email.second) }}
                                    </div>
                                    {{ form_widget(form.register) }}
                                    {{ form_rest(form) }}
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div> 
        </div>
 {% endblock %}
 {% block user_js %}
 <script type="text/javascript">
 $(document).ready(function(){
       
 })
 </script>
 {% endblock %}
 